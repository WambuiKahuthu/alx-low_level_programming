# include "main.h"

/**
 * _isupper - checks if a character is lowercase letter
 *
 * @c: character
 *
 * Return: 1 if uppercase, 0 if not
 */

int _isupper(int c)
{
	if (i >= 65 && i <= 90)
		return (1);
	else
		return (0);

	_putchar('\n');
}
